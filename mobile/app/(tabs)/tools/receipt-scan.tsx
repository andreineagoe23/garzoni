// Tools-hub entry point for the receipt scan screen, which lives at app/scan.tsx
// because it predates the tools registry. Same re-export shim the other tools use.
export { default } from "../../scan";
